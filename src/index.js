import React from 'react'  //Llama a react
import ReactDOM from 'react-dom' //Llama al dom de react 
import App from "./routes/App"; //Llama al componentede react que creamos

ReactDOM.render(<App />, document.getElementById("app")); //Donde va hacer la insercion de lo escrito en react 